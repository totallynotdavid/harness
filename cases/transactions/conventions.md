# transactions conventions

Initialized from: README.md

Project-specific rules for cap check and review.
Project instruction files remain authoritative.

## Surfaces

Files that should change together.

```surfaces
# changed-pattern -> required-pattern
# ^src/cli/ -> ^docs/
```

## House norms

Project-specific rules that change or extend the default checks.

## Subsystem invariants

System rules that cannot be detected from a diff alone.

## Gate-miss ledger

cap deliver writes an entry for every blocker or major finding raised after an
earlier gate round passed the same bytes. Add human-caught misses by hand.

- 2026-09-17 rqueue-task-declare: gate A (haiku) passed `src/rqueue/migrations/0004_retry_policy.sql` at eaa7a80; gate B (luna) then found, in the same bytes: [major] `src/rqueue/migrations/0004_retry_policy.sql:10` The new shared retry-policy state lacks a complete written invariant defining all states and which producer, worker, or operator transition may move between them. The change only partially documents NULL versus persisted policy behavior, leaving cross-process ownership and consistency rules under-specified. (finding 6c3902ec, missed by gate A)

- 2026-09-18 rqueue-role-model: gate A (haiku) passed `src/rqueue/storage.py` at eaa7a80; gate B (luna) then found, in the same bytes: [major] `src/rqueue/storage.py:1245` Whole-schema purge queue discovery uses an unbounded GROUP BY scan. Because the horizon includes an entire finished_at tie group, a budget of 10 with 20,000 tied terminal rows produced a sequential scan of all 20,000 rows, violating the documented work-proportional-to-limit bound. (finding 0b2b59ed, missed by gate A)
- 2026-09-18 rqueue-role-model: gate A (haiku) passed `src/rqueue/roles.py` at eaa7a80; gate B (luna) then found, in the same bytes: [major] `src/rqueue/roles.py:408` provision_role performs multiple DDL and grant-table writes without an encompassing transaction. When a later PURGE grant failed because the routine was absent, the role remained created with earlier jobs privileges but zero queue grants, leaving a partially provisioned role. (finding 5154edc4, missed by gate A)
- 2026-09-18 rqueue-role-model: gate A (haiku) passed `src/rqueue/roles.py` at eaa7a80; gate B (luna) then found, in the same bytes: [major] `src/rqueue/roles.py:442` provision_role() and revoke_role() use bare REVOKE statements, which do not remove ACL entries granted by another grantor. A third-party DELETE grant survived CONSUME-only reprovision (has DELETE after CONSUME reprovision: True), and revoke_role(drop=True) then could not drop the role because the dependent table privilege remained. Least-privilege repair and revocation are therefore incomplete. (finding 34354e2f, missed by gate A)
- 2026-09-18 rqueue-role-model: gate A (haiku) passed `REQUIREMENTS.md` at eaa7a80; gate B (luna) then found, in the same bytes: [major] `REQUIREMENTS.md:416` The change adds concurrent durable state in slots, heartbeats, schedules, occurrences, and purge operations, but the brief does not state the complete state sets, transition owners, or transition timing for these shared states. This violates the required shared-state invariant and leaves concurrency behavior unauditable. (finding 3bac0d70, missed by gate A)
- 2026-09-18 rqueue-role-model: gate A (haiku) passed `src/rqueue/migrations/0005_queue_scoped_runtime.sql` at 5e9db15; gate B (luna) then found, in the same bytes: [major] `src/rqueue/migrations/0005_queue_scoped_runtime.sql:228` The runtime_heartbeats policy authorizes rows only by queue, while worker and scheduler capabilities can insert/update/delete heartbeat rows. A component can forge another kind or instance's liveness, or erase it, making readiness reports untrustworthy and violating the stated ownership invariant. (finding 32bf3b00, missed by gate A)
- 2026-09-18 rqueue-role-model: gate A (haiku) passed `src/rqueue/scheduler.py` at 5e9db15; gate B (luna) then found, in the same bytes: [major] `src/rqueue/scheduler.py:223` Every scheduler tick now executes clear_runtime_heartbeats, which requires DELETE on runtime_heartbeats, but migration 0005 does not upgrade roles provisioned under the previous SCHEDULE grant. Existing scheduler roles therefore fail every tick with InsufficientPrivilegeError: permission denied for table runtime_heartbeats and retry forever. (finding 70985a96, missed by gate A)
- 2026-09-18 rqueue-role-model: gate A (haiku) passed `src/rqueue/migrations/0008_heartbeat_ownership.sql` at 5e9db15; gate B (luna) then found, in the same bytes: [major] `src/rqueue/migrations/0008_heartbeat_ownership.sql:125` The heartbeat INSERT policy queries role_runtime_kinds, but migration 0008 does not grant existing runtime roles SELECT on that new table. An existing worker or scheduler role therefore fails its first heartbeat with permission denied for table role_runtime_kinds and stops functioning after the migration. (finding 16a8d3e7, missed by gate A)
- 2026-09-18 rqueue-role-model: gate A (haiku) passed `src/rqueue/roles.py` at 5e9db15; gate B (luna) then found, in the same bytes: [major] `src/rqueue/roles.py:241` Membership repair emits REVOKE ... GRANTED BY ..., which this source identifies as PostgreSQL 16 syntax, while REQUIREMENTS.md supports PostgreSQL 14+. On PostgreSQL 14/15, provisioning an existing role with memberships fails at this statement, preventing role repair and least-privilege hardening. (finding 60405915, missed by gate A)
- 2026-09-18 rqueue-role-model: gate A (haiku) passed `src/rqueue/roles.py` at 5e9db15; gate B (luna) then found, in the same bytes: [major] `src/rqueue/roles.py:485` Provisioning revokes privileges on tables but never on sequences. A previously over-granted role can retain USAGE or UPDATE on the identity sequences behind jobs.seq and job_attempts.id, allowing sequence advancement/reset and causing ordering anomalies or insert failures despite the role being reported as repaired. (finding 028ec473, missed by gate A)
- 2026-09-18 rqueue-role-model: gate A (haiku) passed `src/rqueue/migrations/0008_heartbeat_ownership.sql` at 5e9db15; gate B (luna) then found, in the same bytes: [major] `src/rqueue/migrations/0008_heartbeat_ownership.sql:198` The DELETE policy requires a current queue grant. When a scheduler role is re-provisioned with a narrower queue set, its old heartbeat is hidden by the SELECT policy and cannot be deleted, so readiness reports the removed queue as served until the staleness window expires. (finding 27df9a0d, missed by gate A)
- 2026-09-18 rqueue-role-model: gate A (haiku) passed `src/rqueue/migrations/0008_heartbeat_ownership.sql` at 5e9db15; gate B (luna) then found, in the same bytes: [major] `src/rqueue/migrations/0008_heartbeat_ownership.sql:44` The migration replaces the runtime_heartbeats primary key with (kind, instance, queue, role_name) without handling already-running v7 runtimes that still execute ON CONFLICT (kind, instance, queue); after upgrading, their heartbeat upsert fails with InvalidColumnReferenceError: there is no unique or exclusion constraint matching the ON CONFLICT specification, stalling worker and scheduler ticks. (finding 02cb343d, missed by gate A)
- 2026-09-18 rqueue-role-model: gate A (haiku) passed `src/rqueue/migrations/0005_queue_scoped_runtime.sql` at 5e9db15; gate B (luna) then found, in the same bytes: [major] `src/rqueue/migrations/0005_queue_scoped_runtime.sql:183` The concurrency_slots RLS policy checks only queue membership while consume roles retain UPDATE and DELETE access, so one worker role can delete another worker's live slot in the same queue; the probe returned DELETE 1, allowing the key to be acquired concurrently. (finding 1b4897e2, missed by gate A)
- 2026-09-18 rqueue-role-model: gate A (haiku) passed `REQUIREMENTS.md` at 5e9db15; gate B (luna) then found, in the same bytes: [major] `REQUIREMENTS.md:528` The new role_runtime_kinds shared state has no complete state set, transition ownership, or ordering invariant; the single capability statement does not specify how migration backfill, provisioning, and capability narrowing interact, so the authorization and cleanup behavior cannot be checked against the required shared-state rule. (finding 0ccdd41d, missed by gate A)
- 2026-09-18 rqueue-role-model: gate A (haiku) passed `src/rqueue/migrations/0008_heartbeat_ownership.sql` at 5e9db15; gate B (luna) then found, in the same bytes: [major] `src/rqueue/migrations/0008_heartbeat_ownership.sql:41` The migration replaces the heartbeat primary key with (kind, instance, queue, role_name) without a preflight or compatibility window. A worker running the previous SQL still uses ON CONFLICT (kind, instance, queue), which fails with InvalidColumnReferenceError after the migration and causes its worker loop to retry indefinitely without claiming or scheduling work. (finding 029762d3, missed by gate A)
- 2026-09-18 rqueue-role-model: gate A (haiku) passed `src/rqueue/migrations/0010_slot_ownership.sql` at 5e9db15; gate B (luna) then found, in the same bytes: [major] `src/rqueue/migrations/0010_slot_ownership.sql:20` Existing live slots are assigned role_name = current_user during migration. Their active workers can no longer UPDATE or DELETE those slots, so the slot expires while the job lease is extended; another worker can then acquire the same concurrency key and run concurrently with the original worker. (finding 5d7b8aee, missed by gate A)
- 2026-09-18 rqueue-role-model: gate A (haiku) passed `README.md` at 5e9db15; gate B (luna) then found, in the same bytes: [major] `README.md:134` The upgrade guidance claims the 0005 drain remedy also covers the 0008 heartbeat ownership change and that applying all pending migrations is safe. This is wrong when the database is already at 0007, because 0005 will not rerun and old workers fail against the new heartbeat key. (finding ba8244bf, missed by gate A)
- 2026-09-18 rqueue-role-model: gate A (haiku) passed `README.md` at 5e9db15; gate B (luna) then found, in the same bytes: [major] `README.md:58` The documentation identifies 0005 as the only migration requiring a stopped fleet, but applying 0010 to a 0009 schema with active slots breaks slot extension and release, allowing duplicate ownership after expiry. (finding b6d788c5, missed by gate A)
- 2026-09-18 rqueue-role-model: gate A (haiku) passed `src/rqueue/roles.py` at 5e9db15; gate B (luna) then found, in the same bytes: [major] `src/rqueue/roles.py:100` The new UPDATE (updated_at) grant is applied only when provision_role is rerun. Existing scheduler roles lack it, so the current enqueue ON CONFLICT path fails with `InsufficientPrivilegeError: permission denied for table jobs`; Scheduler retries and fires no jobs. (finding b7cb7cf8, missed by gate A)
- 2026-09-18 rqueue-role-model: gate A (haiku) passed `src/rqueue/roles.py` at 5e9db15; gate B (luna) then found, in the same bytes: [major] `src/rqueue/roles.py:324` _reject_owned_objects does not inspect pg_collation. A role owning a collation in the queue schema is accepted by provision_role and retains ALTER/DROP ownership authority, violating the documented hardening invariant. (finding 6b1979a6, missed by gate A)
