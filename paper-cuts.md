# Paper cuts

Friction in Captain itself: harness quirks, broken scripts, and skills that misfire.

Generated by `cap papercut` from `paper-cuts.jsonl`. Log with
`cap papercut add <subject> "<text>"`; remove fixed entries with
`cap papercut close <id>`.

A defect in a project Captain was working on is not a paper cut. It goes in
`notes/<project>/defects.md`, where the next task on that project will find it,
and `cap papercut` refuses a project as a subject.

A defect belongs here only while it is open.

- [ ] #1 2026-08-30 `unsorted` - https://github.com/anthropics/claude-code/issues/82563: prompt cache behavior changes between adjacent turns. Captain cannot correct the client behavior. Recheck after a CLI release.
- [ ] #2 2026-08-30 `unsorted` - https://github.com/anthropics/claude-code/issues/89327: the skill catalog is sent twice per session. Keep installed skill snapshots small until the client changes.
- [ ] #3 2026-09-17 `cleanup` - cap cleanup has now stripped a genuine non-obvious-constraint comment/docstring twice in one session on the windows project: once the DUPLICADO enum comment in models.py (docs-centralize task), once the _construir_configuracion docstring explaining why the settings Toplevel is withdrawn-not-destroyed (roster-signal task) — both caught only by live git-diff inspection before commit, both corrected via cap send. The cleanup consult seems biased toward treating any comment near a short/clearly-named function as redundant, without checking whether it documents a cross-cutting invariant (like a threading/lifecycle constraint) that the function's name and body don't express. Consider tightening the cleanup prompt in the agents skill or cap-cleanup to explicitly ask 'does this comment explain something a background thread, a different caller, or a later maintainer could not infer from the code alone' before removing it.
- [ ] #4 2026-09-19 `deliver` - cap deliver --merge merges with gh pr merge, which GitHub rejects for any PR that gh stack link registered in a stack ("must be merged using the asynchronous merge REST API"). gh stack merge <pr> is the supported path: it merges the stack up to that PR atomically, then GitHub retargets and rebases the rest (observed: base moved to master, branch force-updated). cap deliver never calls it and its cascade (stack_cascade_delivered) is not reconciled with it. Landed vendeya stack 125 by hand with gh stack merge 119 --yes --rebase. Also undiagnosed: gh stack link warned on a re-run of cap deliver.
- [ ] #5 2026-09-19 `commit` - cap commit and cap deliver cannot ship a second round on a branch that already has commits. The plan must put every path in exactly one commit and is checked against base..HEAD, so a round that touches an already-committed file can never verify, and cap commit overwrites the plan with only the new round (7 planned vs 168 changed paths). Hit on vendeya-tenant-foundation after the CodeQL fix. The commits were written, then pushed by hand after checking commit_rule_report and ai_trailer_report.
- [ ] #6 2026-09-19 `deliver` - cap deliver's default merge (rebase, docs/commands.md) and gh stack merge --rebase re-create commits on GitHub and drop their signatures. Landing vendeya stack 125 turned 33 locally signed commits into 33 unsigned ones on chatbot master (API: verified=false, reason=unsigned); the 233 commits before them are all verified. The repo's ruleset (disabled) lists required_signatures and required_linear_history, which only a squash or a signed fast-forward push satisfies. Pick the default, and have deliver check signatures after it lands.
- [ ] #7 2026-09-19 `drop` - cap commit keeps its log/<slug>-<timestamp> snapshot ref when a rewrite fails, and neither cap drop nor cap deliver removes it. 11 log/vendeya-tenant-foundation-* branches remain in chatbot after that task was delivered and dropped. drop should delete log/<slug>-*.
- [ ] #8 2026-09-19 `check` - An early-exit consumer in a pipe SIGPIPEs its producer under pipefail. cap-check already documents one (grep -q, line 247) and 2170696 fixed a second (awk exit on a 168-file diff), so the class recurred and no tests/static/lint-* covers it. Neither 2170696 nor 45d9893 (deliver's parent-PR fallback) has a regression test.
- [ ] #9 2026-09-19 `commit` - cap commit cannot take a captain-written plan. Its consult grouped by directory, bundled unrelated steps, and used the rejected docs: prefix despite the prompt forbidding it. Worked around by running a scratch copy of cap-commit with the plan read from a file (CAP_PLAN_ANSWER). A --plan FILE option would remove the workaround.
- [ ] #11 2026-09-19 `commit` - cap commit's plan check is lexical only, so it accepts what rules/commits.md forbids. On vendeya-webhook-signature the first plan used a test: prefix (caught), and the retry passed with a non-imperative subject ("now syncs", wrong verb for an await), bodies that restate the diff and pad ("Comprehensive testing ensures..."), and a test file grouped in an earlier commit than the code it tests. Rebuilt the history by hand as 3 commits before delivery. The consult prompt should get the body and grouping rules, and the check should flag a first line that is not an imperative verb.
