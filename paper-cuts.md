# Paper cuts

Friction in Captain itself: harness quirks, broken scripts, and skills that misfire.

A defect in a project Captain was working on is not a paper cut. It goes in
`notes/<project>/defects.md`, where the next task on that project will find it.
The ledger drifted from this once and carried seven relq and rqueue bugs that no
Captain change could ever close.

Closed entries stay. The "Fixed:" paragraphs are the only written record of why
several of these scripts are shaped the way they are, and other files cite them.

- [ ] 2026-08-30 https://github.com/anthropics/claude-code/issues/82563: prompt cache behavior changes between adjacent turns. Captain cannot correct the client behavior. Recheck after a CLI release.
- [ ] 2026-08-30 https://github.com/anthropics/claude-code/issues/89327: the skill catalog is sent twice per session. Keep installed skill snapshots small until the client changes.
- [ ] 2026-09-01 - comparing two captain hosts (local vs a remote instance) required a dozen manual ssh probes (tool PATH, herdr env vars, gh auth status, bin/ checksums) - there is no single command to audit or diff a host's captain environment
- [x] 2026-09-04 - cap-land's PR-creation retry loop (bin/cap-land, pr mode) is too short: 5 attempts x 3s sleep = 15s max, but gh pr create failed on both relq-conflict-where and relq-conflict-arity within that window today (remote push had just completed), then succeeded immediately when run manually seconds later. Both landings had to be finished by hand (gh pr create run directly, then task.env's CAP_PR field patched in with task_env_set) since cap-land died with 'could not open a PR' after the branch was already pushed. Consider a longer backoff or more attempts before giving up. Fixed 2026-09-09: hit again twice more (relq-migrate-package, relq-foundation-query-shapes) before the fix landed. Retry loop now backs off up to 8 attempts (2,4,6,...16s) and surfaces gh's real stderr in the die message instead of discarding it.
- [x] 2026-09-05 - cap fleet reports a task as 'working' even after its herdr pane has vanished from herdr's live registry entirely (herdr pane list didn't include it at all, not even as closed/dead) - relq-lower-floor showed 'working' in cap fleet while its pane (w4:p21) was completely absent from 'herdr pane list'. The user noticed by looking at the actual herdr panels before I did. cap-fleet's status check apparently trusts stale/cached state rather than confirming the pane still exists live. In this case the underlying work had actually completed correctly (verified directly via the worktree diff), so it wasn't silent data loss, but the status display was actively misleading about whether the task was still running. Fixed: cap-crew's status_of now calls pane_live, which asks herdr for the pane, before it prints working or idle, and prints exited when the pane is gone. Confirmed 2026-09-10 with a task record pointing at a pane herdr answers pane_not_found for: cap crew reports exited.
- [x] 2026-09-08 - cap map regenerates map.md from local disk only, so running it on a machine that holds a subset of the projects silently deletes every row for repos checked out elsewhere. Hit while registering classroom: the run cut map.md from 17 rows to 1. projects.tsv survived because --sync only appends. Either merge the scan into the existing table instead of overwriting it, or make map.md per-machine. Fixed 2026-09-09: map.md is machine state and is now generated and untracked, while config/projects.tsv stays tracked and travels. The boundary is the rule, not the taste: everything that must survive a clone to another machine lives in git, everything that describes this box does not.
- [x] 2026-09-09 - `[ cond ] && cmd` as a bare statement (not an if/while condition, no trailing `|| true`) silently aborts the whole script under captain's `set -e` whenever cond is false, because the bare list's own exit status is the test's false result. Hit twice for real today: bin/cap-ask's last line (`[ -n "$resume_file" ] && rm -f ...`) made cap-ask exit 1 after every successful non-claude call, discarding Gate B's verdict inside cap-gate's pipefail (fixed, see git log); the fix I first wrote for pane_send's chunking loop (`[ "$i" -lt "$n" ] && sleep 0.15`) reproduced the identical failure immediately (also fixed, both now use `if`). `grep -rn '^\s*\[ .* \] &&' bin/ | grep -v '||'` turns up 50+ more matches across cap-skills, cap-gate, cap-spawn, cap-reconcile, cap-drop, cap-land, cap-verify, cap-restack, cap-explore, cap-watch, cap-check, cap-map, cap-crew, cap-conventions, and lib.sh itself - most are probably fine (if/while conditions are exempt, some already have a following command that masks it), but nobody has actually gone through and confirmed which. Needs a real audit, one at a time, checking whether each is actually reachable with a false condition and whether that's the last command in its context - not a blind find-and-replace to `if`, since some of these are deliberately using the short-circuit for a real conditional side effect and blindly converting could change behavior in a way that's harder to spot than the current bug. Fixed 2026-09-10: the premise was too broad. Bash exempts the test in `a && b` from set -e, so the shape is harmless mid-body; it is only fatal in tail position, where a false test becomes a script's exit status or a function's return value. Of the 68 matches in bin/, two were real: bin/cap-conventions' last line made `cap conventions` exit 1 after successfully writing the file whenever the project had none of AGENTS.md/CLAUDE.md/CONTRIBUTING.md/README.md, and lib.sh's pane_enter, which cap-send calls bare. The other 66 are mid-body, or predicates only ever asked inside a condition, which is the correct way to write that shape. shellcheck reports nothing at any level, so bin/lint-andlist now decides it: a candidate is flagged when every significant line after it out to the end of its function or file is a block closer, and a function tail is only a defect when some call site invokes it bare. It runs as mise's check:andlist, and it reproduces both of the original failures on fixtures.
- [x] 2026-09-09 - cap verify runs only the first matching task when that task reads stdin. Its loop feeds task names in with a here-string, and mise run check (docker compose underneath) consumes the rest, so check:certificate never ran and the command still exited 0. A verification tool silently skipping half its checks is worse than one that fails. Fixed 2026-09-09: cap-verify now iterates an array and runs every check with stdin closed, and reports how many tasks it ran. bin/cap-gate had the identical printf|while shape and would have skipped Gate B once cap-ask consumed the profile list; fixed the same way. See notes/captain/harness-review.md section 7.
- [x] 2026-09-09 - cap gate has no lock, so two runs on the same task race. Found live on local-env: PIDs 296983 and 306107 were both running 'cap-gate local-env --full' at 11:12, each with its own Gate A opus session, both tee-ing to the same notes/classroom/local-env.gate-A.md and both about to write the same state/tasks/local-env/gate.json. Whichever finishes second wins the report, and gate_record's read-modify-write on gate.json can lose the other profile's verdict entirely. It also pays twice for the same review. Fix: take a lock on state/tasks/<slug>/ in cap-gate and refuse a second concurrent run for that slug, the way cap-spawn already refuses a duplicate pane. Fixed 2026-09-09: lib.sh grew task_lock, an flock on state/tasks/<slug>/.lock that cap-gate takes right after task_load, so a second run on the same slug dies naming the pid that holds it. The same round fixed the other half of the damage: cap-gate opened the report with `tee "$out"`, which truncated the last good review before the new session had produced a byte, so a session that died on a quota rejection destroyed it. It now tees to a temp and promotes it only once gate_verdict finds PASS or FAIL. local-env.gate-A.md was found at zero bytes today and restored from git.
- [x] 2026-09-09 - Captain measured no codex quota. Dispatch sizing reads rate-limit windows from what the harness hands its status line (bin/cap-statusline), and that is a Claude Code hook with no codex equivalent, so `usage_read codex` returned unmeasured; cap-ask's session-limit detection was claude-gated too, so a codex rejection blocked nothing. Fixed 2026-09-09: codex writes the same information to disk without being asked. Every turn appends a `token_count` event to its rollout under `~/.codex/sessions/`, carrying `rate_limits` with `primary` (300-minute window) and `secondary` (10080-minute window), each with `used_percent` and `resets_at`, plus a `rate_limit_reached_type` that is non-null once a window is exhausted. Confirmed against a real session on codex-cli 0.153.4 (gpt-5.6-luna at xhigh), which reported primary 4% and secondary 31%. `usage_read codex` now reads the newest rollout and cap-ask blocks a codex profile when `rate_limit_reached_type` is set. The record also carries `plan_type`; Captain deliberately does not read it. Improved 2026-09-09: the rollout is now the fallback, not the source. `codex app-server` answers `account/rateLimits/read` with the windows as they stand right now, which matters because the moment Captain most needs to know whether codex has room is the moment no codex session is running and the newest rollout is hours old. It is one JSON-RPC round trip over stdio, cached for `CAP_USAGE_TTL`, and the reading carries its own source so `cap budget` says which one a number came from.
- [x] 2026-09-09 - `CAP_ASK_PROFILES` is the one setting in config/captain.conf that an environment variable cannot override. Every other value uses `: "${VAR:=default}"`; the profile table is a plain assignment, so `CAP_ASK_PROFILES=... cap models` silently uses the file's table instead. Testing a profile change means editing the file and putting it back. Not urgent, but the header of that file claims all of it can be overridden, and right now that is not true. Fixed 2026-09-10: the table is now `: "${CAP_ASK_PROFILES:=...}"` like every other value, so the file's header is true again. Confirmed with `CAP_ASK_PROFILES='probe claude haiku -' cap models`, which drops every codex profile from the catalog's PROFILE column.
- [ ] 2026-09-09 - Mined every failing tool call across all 15 session transcripts (69 distinct failures). 16 of them, 23%, are an agent discovering the worktree lacks something the task obviously needed: docker (twice, WSL2 integration off), php, pdfinfo, a GPG signing key, a .env.example that only existed in a sibling branch, and a localhost dev server nobody started. cap-spawn creates the worktree and declares it ready without checking that the project can actually be built or run in it, so the agent finds out one failed turn at a time. A per-project preflight (the same place cap-verify already reads the project's own check tasks from) should run at spawn and either fix or report what is missing before the agent is told to start. Note that a further 5 'missing package' failures are not missing at all: agents wrote scratch .mjs files into the session scratchpad, which is outside the worktree, so node could not resolve node_modules. That half is a briefing problem, not a provisioning one.
- [x] 2026-09-09 - bash expands every word of a 'local' builtin before it assigns any of them, so 'local slug=$1 dir=$TASKS/$slug' dies under set -u with 'slug: unbound variable'. Hit while adding task_lock to lib.sh. Same family as the '[ cond ] && cmd' papercut from earlier today: a shell footgun that only fires under captain's own set -euo pipefail, and only on the branch nobody exercised. Every other multi-assignment local in lib.sh happens to reference $1 directly rather than an earlier variable, so none of them are affected today, but nothing stops the next one. 'grep -nE "local [a-z_]+=[^ ]+ [a-z_]+=.*\$[a-z_]" bin/' finds the shape. Fixed 2026-09-09: shellcheck calls this SC2318 and catches it. Captain now has a mise.toml with lint and check:syntax tasks that cap verify picks up, and it caught a third instance in preflight_tools that had been written an hour after the first was fixed. The '[ cond ] && cmd' audit is still open; shellcheck does not flag that one.
- [x] 2026-09-09 - cap-ask's resume key could not tell one gate round from the next, so a gate resumed a finished review and restated its verdict against code it had never read. The key is sha256(profile, dir, prompt), and cap-gate sends a byte-identical review prompt every round from the same worktree with the same profile. local-env's Gate A hit its session limit at 11:51 after reaching GATE: FAIL on a missing executable bit; the round-five gate at 16:16 resumed that session, got its old conclusion restated verbatim (the capture still carried the 11:51 timestamp), and recorded FAIL against a fingerprint whose worktree had the bit set. The report file came back byte-identical to the committed one, which is what gave it away. Gate B, which had no pending resume, genuinely re-ran and passed. Fixed 2026-09-09: cap-ask folds an optional CAP_ASK_KEY into the resume key and cap-gate passes the worktree fingerprint, so a resume can only ever continue a review of the exact code that was interrupted. Any other caller whose prompt is constant across calls that mean different things must supply its own discriminator.
- [x] 2026-09-09 - `cap send` gives up when the task's pane is gone: it prints "pane 'local-env' is not live" and exits, so the message is lost and the captain is left holding work the agent should be doing. Hit on local-env, whose pane had died while the task itself was still open and mid-round; the correction meant for the agent had to be applied by hand instead. The task record still holds everything needed to bring the session back (CAP_TREE, CAP_BRANCH, CAP_PANE, CAP_MODEL, CAP_HARNESS in state/tasks/<slug>/task.env), which is how cap-spawn opens one in the first place. cap-send should detect the dead pane, relaunch the session against the same worktree with the task's own brief and status log as context, and then deliver the message, rather than making a dead pane the captain's problem. Worth deciding whether it revives silently or announces the restart, since a revived session has lost the conversation the original was carrying. Fixed 2026-09-09: cap-send revives the session from task.env (same worktree, same model, same harness) and puts the captain's message inside the opening prompt, after pointing the new session at its contract and status log. The message goes in the prompt rather than arriving as a second turn, because a freshly launched pane is already busy. It announces the restart, since a revived session has lost the conversation the original was carrying.
- [x] 2026-09-09 - cap-spawn writes the path-ownership hook to <worktree>/.claude/settings.local.json and excludes it via the repo's .git/info/exclude, but that mechanism fails open twice over. Linked worktrees do not read their own $GIT_DIR/info/exclude (confirmed empirically on aula-tooling: a pattern written to .git/worktrees/<slug>/info/exclude is ignored, only $GIT_COMMON_DIR/info/exclude is consulted), so the pattern has to go in the shared file every worktree and the primary checkout also read. And that shared file is not owned by Captain: the entry written for aula at 16:xx was gone by 17:xx, leaving .claude/ untracked in the aula-tooling worktree where a 'git add .' would have committed Captain's hook config into the project. Restored by hand for now with a broader '.claude/' pattern. A real fix should not put a file in the worktree at all, or should re-assert the exclude on every cap command that touches the task rather than only at spawn. Fixed 2026-09-10: nothing goes into the worktree at all. claude's `--settings` takes the JSON itself rather than a path, so cap-spawn builds the guard config into the session's command line and neither writes .claude/settings.local.json nor touches any info/exclude. Confirmed end to end on a probe task owning src/**: a Write to README.md is blocked by the hook and a Write to src/new.txt succeeds, with no .claude/ in the worktree and nothing but the agent's own file in git status. The guard is a claude hook and has no codex equivalent, which was equally true of the old file; cap-spawn now warns when --owns is passed with a codex harness instead of silently not enforcing it.
- [x] 2026-09-09 - gate_verdict took a fixed `tail -15` of the report, so a pane capture that ended with a dozen blank lines below the verdict lost it. Gate B genuinely reviewed local-env and printed GATE: PASS, and cap-gate recorded UNKNOWN, kept the previous round's report and told the captain to re-run a gate that had already done its work. The blank tail is normal: how much empty space a capture carries below the last line depends on the pane, not on the review. Fixed 2026-09-09: blank lines are dropped before the window is taken, so the window holds the last fifteen lines that say anything.
- [x] 2026-09-10 - `printf '%s\n' "$big" | grep -q pattern` under captain's own `set -euo pipefail` can report a real match as "not found" whenever the match isn't on the last line of a large buffer. `grep -q` exits the instant it finds a match, before `printf` finishes writing the rest into the pipe; `printf` then dies of SIGPIPE (141), and pipefail reports that 141 as the pipeline's exit status instead of grep's real 0. Found chasing a false positive in cap-check's stale-symbol scan: relq-executor-ergonomics' diff genuinely re-added `__init__` twelve times, but the 74KB diff made the match land well before the end, so `cap check` reported the method as removed-and-still-referenced. Reproduced deterministically (4/4) once isolated from `cap-check`'s real code path, and confirmed the failure disappears with a here-string instead of a pipe. `bin/cap-ask`'s session-limit check (`printf '%s' "$tail_from_prompt" | grep -qF "hit your session limit"`) has the identical shape against a buffer that can run to ~1000 lines - a genuine rejection banner occurring early in that capture could have gone undetected the same way, silently letting a truncated response pass as real. Fixed 2026-09-10: both switched to `grep ... <<<"$var"`, which has no pipe to break. `grep -rn '| grep -q' bin/` turned up three more (cap-check's own surface-map check, lib.sh's `owns_regex` check, cap-send's chunk-delivery check) but those pipe short, single-path/filename-sized strings that fit in one pipe-buffer write, so the race window in practice is far narrower; left alone rather than converting everything on spec.
- [ ] 2026-09-10 - `--lines 1000` (herdr's own hard ceiling, see the entry below) is not actually a fix for long reviews, only a bigger window - a review thorough enough to run 8+ minutes with heavy tool use can still exceed 999 lines of terminal output on its own, independent of scrollback size. rqueue-task-declare's Gate A did this today: the capture kept the tail (findings 3-5, verdict, cost line) and lost the header plus findings 1-2, including the most serious finding in the report (`declare_task()` unusable on any queue with a customised `default_retry`, and order-dependent on top of that). Recovered from the session transcript the same way. herdr's `--lines` ceiling means captain cannot fix this by asking for more - a seventh instance of the pane-scraping class notes/captain/briefs/cap-headless-ask.md already targets; the brief's headless JSONL `result` field has no viewport or line-count limit at all, so this stops mattering once that lands rather than needing its own fix.
- [x] 2026-09-09 - cap-ask's `herdr pane read` call (bin/cap-ask) had no `--lines` argument, so it defaulted to reading only the visible viewport (~80 lines) instead of the scrollback. Any gate review longer than that lost its *earliest* lines while keeping the tail (verdict, cost line) intact - the truncation looked like a normal, complete report. Confirmed on two separate real gate rounds today: relq-executor-ergonomics' gate-A.md was missing findings 1-5 (including a breaking API change in `transaction()` and an exception-misattribution bug in `fetch_iter`), and rqueue-role-model's gate-A.md was missing findings 1-2 (both real regressions: `purge(queue=None)` not actually draining oldest-first across the schema despite the just-added REQUIREMENTS.md claim, and the same purge losing its bounded-scan behavior, now reading the entire retention backlog on every call). Recovered the full text both times from the claude harness's own on-disk session transcript (`~/.claude/projects/<sanitized-worktree-path>/*.jsonl`), since herdr's ephemeral gate panes had already closed by the time the truncation was noticed. Fixed by adding `--lines 1000` (herdr's own hard ceiling for this call - anything requested above it returns the same 999 lines back). `pane_tail` in lib.sh already passes an explicit `--lines` and was not affected.
- [x] 2026-09-10 - cap check's em-dash scan reads the diff, so it cannot tell our own added prose from a verbatim copy of an external file that must match byte-for-byte. classroom's .devstack/generate-config.sh carries a STRIP_LINES heredoc holding two comment lines lifted exactly from .cicat/config.php.example, em dashes included; the strip and its self-verification both match those lines literally, so "fixing" the punctuation would silently stop the strip from finding them. The scan flags them on every round, and the only way to clear it is to remember why not to. A file-level or line-level opt-out (a marker comment, or skipping heredocs whose content is asserted equal to a tracked file) would stop the check from training the captain to ignore it. Fixed 2026-09-10 by deleting the scan. The character was standing in for a judgment about sentence structure: it caught one punctuation form of a two-idea sentence, missed every sentence that does the same with a comma, and produced this false-positive class of its own. The opt-out written for it needed a marker syntax and a rule for verifying the marker's claim, which is a lot of machinery to defend a proxy nobody wanted measured. Readability already has an owner. AGENTS.md leads with plain writing, rules/comments.md carries the checklist, and cap cleanup runs a model pass over both on every task. NORTH.md says repeated deterministic checks belong in scripts and infrequent judgment belongs in a focused model pass; this was judgment in a grep.
- [ ] 2026-09-10 - `pgrep -f` is unusable for polling task processes once `cap send` has revived a session, because the revive puts the captain's entire message inside the launch prompt and the harness carries that prompt in its argv. A gate round's instructions run to several thousand characters and quote the exact commands the agent is told to run, so `pgrep -f 'mise run check'` matched the claude session itself, not the check. Hit twice in a row today on local-env: two `until ! pgrep -f ...; do sleep; done` waiters never terminated because the agent's own command line kept matching, and one had to be killed by hand. Anything that waits on a task's real work should watch the work's own output file or pid, or at least anchor the pattern to the binary (`pgrep -f '^mise '`, `pgrep -x`), never a phrase that could appear in a brief or a captain message. Worth considering whether cap-send should pass the revival text through a file or stdin instead of argv, since argv is world-readable on this box and the same message can carry findings that quote source.
- [ ] 2026-09-10 - killing cap-gate does not kill the review it launched, so the account keeps paying for an Opus session whose verdict nobody will ever record. herdr starts the reviewer in its own pane, detached from cap-gate's process tree, so when the harness's low-memory guard killed the backgrounded `cap gate local-env --full` seconds after it started, the wrapper died and the opus process (pid 1637014, pane w2:p1E) carried on reviewing for another ten minutes with nothing left to read its output, promote the report or call gate_record. Recovered by hand this time: waited on the orphan's pid, read the pane, wrote notes/classroom/local-env.gate-A.md and called gate_record with the fingerprint directly. The same shape applies to any interruption of cap-gate, Ctrl-C included, and it is worse than a wasted review because state/tasks/<slug>/gate.json still holds the previous round's verdict at a stale fingerprint, which reads exactly like a gate that was never run. cap-gate should either trap EXIT and close the pane it opened, or record enough (pane id, fingerprint, profile) that a later run can adopt an orphaned review instead of paying for it twice.
- [ ] 2026-09-10 - cap cleanup and cap commit run an agent inside a task's worktree with no ownership guard, because the guard is installed by cap spawn (claude --settings on the session's command line) and both of these dispatch through cap-ask, which installs nothing. Hit on cap-headless, whose --owns was 'bin/cap-ask bin/cap-gate bin/lib.sh': the cleanup pass edited bin/cap-check as well, and reported it under a heading of its own as if it were in scope. Worse than untidy, that file had been changed on master since the worktree was cut, so committing the edit would have carried a stale copy of a block master had already deleted back through the merge. Reverted by hand. cap-ask should take the same guard argument cap-spawn builds, whenever it is called with --dir pointing at a task worktree.
- [ ] 2026-09-10 - cap gate's review prompt tells the reviewer to run 'git diff $CAP_BASE', the base tip, so everything landed on the base since the worktree was cut reads as a deletion by the task under review. cap check was fixed for exactly this (commit cbc89f9, 'Diff cap check from the merge base, not the base tip') and lib.sh's sync_base exists to reduce the same class, but the gate prompt was never changed. Cost a real finding today: cap-headless's Gate B reported that the task had deleted a paper-cuts.md entry describing a live unfixed bug, and called it erasing the paper trail. The task had not touched paper-cuts.md at all; the entry was added to master after its branch was cut. Confirmed with 'git diff --stat $(git merge-base master HEAD) -- paper-cuts.md', which is empty. The prompt should name the merge base, the way cap-check computes diff_from.
- [x] 2026-09-10 - cap cleanup removed three comments that carried the reason for the code, while correctly removing about six that only restated the function name. On cap-headless round two the losses were owns_overlap's return convention ('concrete files first, then prefixes for ground neither has touched'), owns_taken's ('prints the claimants and returns 0 when it finds one'), and pane_agent_status's rejected alternative ('herdr's own reported status, not a guess from output staleness'). It also compressed three pre-existing cap-gate comments until they no longer named the failure they prevent: 'every gate after the first silently never runs while the command still exits 0' became 'piping would eat remaining profiles'. The judgment rules/comments.md asks for is the right one and the pass mostly makes it; the misses are all the same shape, a comment whose value is a rejected alternative or a return convention rather than a description of what the code does. Worth naming those two shapes explicitly in the cleanup prompt as things to keep. Not a scope problem: an earlier version of this entry blamed cleanup for reading the whole file rather than the diff, which was wrong. Reading the whole file is how it found the six good deletions. Fixed 2026-09-10: rules/comments.md's audit section now names the three shapes worth keeping when a comment opens with the function name (a return convention, a rejected alternative, an external quirk) and states that deleting is the asymmetric move, since the reviewer reading the diff sees a deletion and no reason to doubt it. The next pass, against cap-headless, shortened every borderline comment instead of deleting it and preserved both the glob-to-regex directory-claim rule and task_lock's re-entrancy contract. It still dropped one caller contract, that a caller reusing a prompt must set CAP_ASK_KEY or resume a stale session, which had to be sent back. Better, not solved.
- [x] 2026-09-10 - cap send refused to send once a session was at or over the 40% context ceiling, and the only way past it was --force, which is the exact thing the ceiling exists to prevent. The other two routes it offered were worse: finishing the task is not possible when the reason for sending is a gate finding, and cap drop plus a fresh cap spawn throws away the worktree. So the captain compacted the pane by hand and sent again, which is a workaround performed by an operator rather than by the command that knows it is needed. Hit on cap-headless round three, where the pane sat at 42% with four small fixes left to apply. Fixed 2026-09-10: cap send now sends /compact itself, waits for the context reading to fall under the ceiling, and then delivers. cap-ask already did this before resuming its own sessions; the two now behave the same way. --force still skips the check, and the die message is kept for the case where compaction genuinely does not bring the session under the ceiling. Verified live against cap-headless with the ceiling lowered to 10: "at 14% context; compacting before the message goes in", "compacted cap-headless to 0% context", "sent to cap-headless (working, ctx 0% used)". The wait keys on the context percentage rather than the agent status, because the status line holds the pre-compaction reading behind the progress bar until the moment compaction ends.
- [ ] 2026-09-10 - A reviewer that cannot run the thing it reviews reports its own uncertainty as a defect, and the report gives the captain no way to tell that apart from a real finding. Gate B on cap-headless raised two findings in round two, both worded as risks the captain should verify before landing: /compact may not work in headless print mode, and usage_write may read wrong rate-limit field paths. Both are wrong, and one live claude -p --resume "/compact" plus one live rate_limit_event settled them in under a minute. Round one B had already invented a deleted paper-cut entry from a stale base. The same profile passed local-env in the same period after running the config generator in an isolated fake repo, reading Moodle core to confirm exit-code branching, and checking that the pinned Docker tags exist, so this is not a weak model. It is a reviewer placed where it could not execute, saying so, and having that read as a verdict. Gate A in the same rounds reproduced three of its four findings against a stubbed harness. Two fixes worth weighing: give the review profile whatever it needs to exercise the code, and make the report separate a reproduced defect from a suspicion so the captain sees which is which without reading every paragraph. The second is plan Stage 3.
- [x] 2026-09-10 - cap crew reported `done` for two tasks that were visibly mid-turn, because status_of reads the last verb in the agent-written status.log and cap send never invalidated it. The UserPromptSubmit reminder hook reads the same field, so it listed four tasks as "done and unlanded" while two of them were actively working, and I nearly concluded an agent had applied four fixes in two minutes. herdr knew the truth the whole time (agent_status was "working"), but a sticky done record means the live state is never consulted. The agent sometimes wrote a `working:` line itself, which is the shape the pinned rule warns about: correctness depending on the agent remembering. Fixed 2026-09-10: cap send appends the `working:` record itself, right after wait_working confirms a turn actually started. That is the one moment the harness knows with certainty that the task is no longer done, so it no longer infers it from the screen or waits for the agent to volunteer it.
- [x] 2026-09-10 - cap cleanup could only ever run before cap commit. Its guard refused a worktree with no uncommitted changes, but its own prompt works against $CAP_BASE, which is just as well defined after the commit. cap check reports over-long comment blocks on committed work and the pass that fixes them was unreachable at that point. Fixed 2026-09-10: the guard now counts the tracked diff against the merge base plus untracked files, and refuses only when both are empty. That alone was not enough, because the prompt spoke of "the tracked diff and every untracked file git status reports" and the pass read git status on a committed tree, saw it clean and reported nothing to do. The prompt now names both commands and says outright that a clean git status does not mean there is nothing to review.
- [x] 2026-09-10 - cap check's siblings section was unreadable on a repository that vendors a framework. It greps the whole tree for each changed file's basename, so on the classroom Moodle worktree the keys "moodle", "deploy", "README" and "default.conf" each matched thousands of files, and head -5 printed the first five with no count. Thirty lines of vendored AWS SDK and Roundcube paths sat where a real lead should have been, which is how a genuine one gets skipped. Fixed 2026-09-10: outside() takes a cap and the siblings check passes CAP_CHECK_SIBLING_MAX (20), so a key matching more files than anyone would open is dropped rather than truncated, and a kept key now prints how many more there are. The cap is deliberately not applied to the stale check that shares the same helper: a removed symbol still referenced in a thousand files is the most serious version of that finding, not the least. Verified both ways, the Moodle worktree's siblings section went from thirty noise lines to ok, and the captain repo still reports cap-check's five real mentions.
- [x] 2026-09-10 - cap cleanup skipped local-env's .devstack/ on two separate passes, seventeen files and the bulk of that task's change, and the earlier note in bin/cap-cleanup blamed the pass for reading only the tracked diff. The pass was not at fault. Its prompt told it to find untracked files with `git status --porcelain`, and git collapses an untracked directory into the single line `?? .devstack/`. Seven porcelain lines where ls-files reports nineteen. Every one of the missing files was invisible to the command the agent was told to run. Fixed 2026-09-10: the prompt names `git diff --name-only <merge-base>` and `git ls-files --others --exclude-standard`, says outright not to use git status for this, and says why. The guard counts the same way. Verified by rerunning the pass on the same worktree: it now cites render-test-certificate.php, generate-config.sh and .devstack/lib.sh by name, none of which it had mentioned on either earlier run. The general lesson is worth more than the fix. When an agent misses something, check what the command it was handed actually returns before concluding the agent skipped it.
- [ ] 2026-09-10 - A verification stub left an artifact in state/ask that the next gate round cited as real captured evidence. Gate A's round-four finding on cap-headless pointed at state/ask/a7195ba1-ca4a-4351-aba7-8a11e86540f5.jsonl as "the repo's own captured rejection" and ran the shipped expression over it. It was garbage written by the previous round's own stubbed claude, left behind because the agent deleted the file matching the fake session id its stub printed rather than the real id cap-ask generated for the transcript filename. The finding happened to be correct, confirmed independently against the shipped expression, so nothing was lost this time. The exposure is that a reviewer cannot tell a real transcript from a stub's leftovers, because state/ask holds both and they are the same shape. cap-ask's 7-day sweep does not help within a session. Two directions worth weighing: give agent-run stubs their own state root through an env var cap-ask already honours, so a test can never write where a reviewer reads, or record provenance in the transcript so a reviewer can tell. The first is the real fix, since the second still depends on the reviewer checking.
- [ ] 2026-09-10 - cap gate paid for two full reviews of an empty diff, and would have printed READY TO LAND for a task with nothing to land. Its guard counts commits (`git rev-list --count $CAP_BASE..HEAD`), which stays above zero for a branch whose content is already in the base, either because sync_base merged the base back in or because the work landed by another route. local-env hit both: its dev stack went onto classroom master as ea3ecec2..41477d00 by hand, the gate's own sync_base then merged master into the branch, and `cap gate local-env --full` ran an opus Gate A and a sonnet Gate B against a diff of nothing. Both verdicts were recorded against fingerprint e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855, which is the SHA-256 of the empty string, and gate_ready compares that to a tree fingerprint that is also the empty hash, so two PASSes there would have read as ready. The review was salvaged only because Gate A noticed the empty diff itself and reviewed the landed range instead. The same blind spot is why the reminder hook still lists local-env as "done and unlanded" with nothing left to land. cap gate should refuse when the reviewable diff is empty and say the work is already in the base, and gate_ready should never treat the empty-string fingerprint as a match. Not fixed here because bin/cap-gate and bin/lib.sh are both owned by the cap-headless task right now.
