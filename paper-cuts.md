# Paper cuts

Friction in Captain itself: harness quirks, broken scripts, and skills that misfire.

Generated by `cap papercut` from `paper-cuts.jsonl`. Log with
`cap papercut add <subject> "<text>"`; remove fixed entries with
`cap papercut close <id>`.

A defect in a project Captain was working on is not a paper cut. It goes in
`notes/<project>/defects.md`, where the next task on that project will find it,
and `cap papercut` refuses a project as a subject.

A defect belongs here only while it is open.

- [ ] #1 2026-08-30 `unsorted` - https://github.com/anthropics/claude-code/issues/82563: prompt cache behavior changes between adjacent turns. Captain cannot correct the client behavior. Recheck after a CLI release.
- [ ] #2 2026-08-30 `unsorted` - https://github.com/anthropics/claude-code/issues/89327: the skill catalog is sent twice per session. Keep installed skill snapshots small until the client changes.
- [ ] #3 2026-09-17 `cleanup` - cap cleanup has now stripped a genuine non-obvious-constraint comment/docstring twice in one session on the windows project: once the DUPLICADO enum comment in models.py (docs-centralize task), once the _construir_configuracion docstring explaining why the settings Toplevel is withdrawn-not-destroyed (roster-signal task) — both caught only by live git-diff inspection before commit, both corrected via cap send. The cleanup consult seems biased toward treating any comment near a short/clearly-named function as redundant, without checking whether it documents a cross-cutting invariant (like a threading/lifecycle constraint) that the function's name and body don't express. Consider tightening the cleanup prompt in the agents skill or cap-cleanup to explicitly ask 'does this comment explain something a background thread, a different caller, or a later maintainer could not infer from the code alone' before removing it.
