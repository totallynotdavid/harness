#!/usr/bin/env bash
# cap-peek - show recent agent output.
. "$(dirname "$(readlink -f "$0")")/lib.sh"

slug=${1:?usage: cap peek <slug> [lines]}

pane_live "$slug" || die "pane '$slug' is not live"

pane_tail "$slug" "${2:-60}"
